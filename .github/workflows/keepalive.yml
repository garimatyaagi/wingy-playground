name: Keepalive

on:
  schedule:
    - cron: "0 0 */21 * *"

jobs:
  keepalive:
    runs-on: ubuntu-latest
    timeout-minutes: 1
    permissions:
      actions: write
    steps:
      - uses: actions/checkout@v4
      - uses: gautamkrishnar/keepalive-workflow@v2
